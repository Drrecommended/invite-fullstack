import React, { useEffect } from 'react';
import { useExample } from '../hooks'
import '../styles/Example.css'

export default () => {
  const {  } = useExample()
  useEffect(() => {
    getExample()
  })
  return (

  )
}